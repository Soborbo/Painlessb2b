---
import Layout from '../layouts/Layout.astro';
import BulkAddPage from '../components/BulkAddPage';
import { SITE_CONFIG } from '../lib/site-config';
---
<Layout title={`Bulk Add — ${SITE_CONFIG.name}`}>
  <BulkAddPage client:only="react" />
</Layout>
